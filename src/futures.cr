# This module contains definitions for `Future` and
# `ExecutionContext`
module Futures
  require "./futures/exceptions"
  require "./futures/execution_context"
  require "./futures/future"
  require "./futures/try"
  require "./futures/option"
  require "./futures/promise"
end